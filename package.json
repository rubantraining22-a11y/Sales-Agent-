{
  "name": "sales-agent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix backend --include=optional sharp @img/sharp-linux-x64 && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  }
}
