{
  "name": "sales-agent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "export ONNXRUNTIME_NODE_INSTALL_CUDA=skip; npm install --prefix backend --omit=optional && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  }
}
